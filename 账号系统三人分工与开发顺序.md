# Yoko 账号系统三人分工与开发顺序

计划日期：2026-08-24

目标：为云端部署增加简单可靠的注册、登录、退出和六个月持久登录能力，并确保每个用户只能访问自己的对话、提醒、记忆、反馈和指标。

## 1. 功能范围

### 1.1 本轮实现

- 用户名和密码注册。
- 用户名和密码登录。
- 退出当前登录会话。
- 页面刷新或浏览器重启后自动恢复登录。
- 登录会话从登录成功时起固定保存 180 天，过期后必须重新登录。
- 同一账号允许在多台设备分别登录。
- 所有业务数据由后端根据当前登录会话确定所属用户。
- 登录失败进行最小限度的频率限制。
- 生产环境使用 HTTPS 和安全 Cookie。
- 保留旧数据库，通过版本化迁移增加账号字段和会话表。

### 1.2 本轮不实现

- 手机号或邮箱验证码。
- 找回密码和修改密码。
- 第三方登录。
- 管理员、家庭成员等多角色权限。
- JWT、刷新令牌或 OAuth 服务。
- 人脸、指纹等生物认证。
- 跨域多站点部署。

这些能力不是当前黑客松演示的必要条件。本轮只实现单一普通用户角色。

## 2. “自动保存账号密码”的定义

系统不保存明文密码，也不把密码放进 `localStorage`、`sessionStorage` 或普通 Cookie。

本轮按以下方式满足自动保存需求：

1. 登录成功后，服务端创建随机会话令牌。
2. 浏览器通过 `HttpOnly` Cookie 保存令牌 180 天。
3. 页面启动时调用 `GET /api/auth/me` 自动恢复登录状态。
4. 登录表单使用 `autocomplete="username"` 和 `autocomplete="current-password"`，允许浏览器密码管理器保存和自动填充账号密码。
5. 注册表单使用 `autocomplete="new-password"`。

Cookie 过期、会话被退出或服务端会话被撤销后，用户必须重新输入密码登录。

## 3. 技术方案

### 3.1 认证方式

使用服务端 Session，不使用 JWT。

- 登录成功时使用安全随机数生成原始 Session Token。
- 原始 Token 只通过 Cookie 返回给浏览器。
- 数据库只保存 Token 的 SHA-256 哈希，不保存原始 Token。
- 每次请求由后端读取 Cookie、哈希后查询会话，并检查是否过期或撤销。
- 密码使用 `pwdlib[argon2]` 生成 Argon2id 哈希。
- 登录成功后不把密码、密码哈希或 Session Token放进 JSON 响应。

选择服务端 Session 的原因：当前是 FastAPI、SQLite 和单体网页应用，Session 更容易立即撤销，也不需要额外设计刷新令牌。

### 3.2 Cookie 规则

Cookie 名称建议：

```text
生产环境：__Host-yoko_session
本地开发：yoko_session
```

属性：

```text
HttpOnly=true
SameSite=lax
Path=/
Max-Age=15552000
Secure=true（生产 HTTPS）
Secure=false（仅本地 http://127.0.0.1 开发）
不设置 Domain
```

`15552000` 秒等于 180 天。本轮使用固定过期时间，不因普通 API 请求自动续期；重新登录会创建新的 180 天会话。

### 3.3 部署约束

生产环境将前端和后端放在同一站点下：

```text
https://example.com/       前端
https://example.com/api/   后端
```

这样可以继续使用 `SameSite=Lax`，减少跨站 Cookie、CORS 和 CSRF 的复杂度。若云平台只能把前后端部署到不同站点，本轮方案需要重新加入 CSRF Token，不得直接把 Cookie 改为 `SameSite=None` 后上线。

### 3.4 数据库变更

新增 V3 迁移，不修改或删除现有用户业务数据。

扩展 `users` 表：

```text
username              TEXT，可空，仅兼容旧 demo-user
username_normalized   TEXT，可空
password_hash         TEXT，可空
disabled              INTEGER NOT NULL DEFAULT 0
last_login_at         TEXT，可空
failed_login_count    INTEGER NOT NULL DEFAULT 0
login_blocked_until   TEXT，可空
```

增加条件唯一索引：有效账号的 `username_normalized` 不允许重复。用户名比较统一使用去除首尾空格后的大小写无关形式。

新增 `auth_sessions` 表：

```text
id             UUID 主键
user_id        外键，关联 users.id
token_hash     TEXT UNIQUE NOT NULL
created_at     ISO 8601 UTC
expires_at     ISO 8601 UTC
last_seen_at   ISO 8601 UTC，可空
revoked_at     ISO 8601 UTC，可空
```

必要索引：

```text
UNIQUE(token_hash)
INDEX(user_id, expires_at)
INDEX(expires_at, revoked_at)
```

旧 `demo-user` 保留但不自动获得公开密码，不允许在生产环境通过固定默认密码登录。测试夹具可以继续创建测试账号。

### 3.5 API 合同

新增接口：

| 方法 | 路径 | 是否需要登录 | 用途 |
| --- | --- | --- | --- |
| `POST` | `/api/auth/register` | 否 | 注册并自动登录 |
| `POST` | `/api/auth/login` | 否 | 登录并设置 Cookie |
| `GET` | `/api/auth/me` | 是 | 获取当前用户并恢复登录 |
| `POST` | `/api/auth/logout` | 否 | 有有效 Session 时撤销；无效或过期时也删除 Cookie 并返回成功 |

建议输入输出：

```text
RegisterRequest
  username: 3-32 个字符
  password: 8-128 个字符
  display_name: 1-32 个字符
  timezone: 默认 Asia/Shanghai

LoginRequest
  username: 3-32 个字符
  password: 8-128 个字符

UserView
  id: UUID
  username: string
  display_name: string
  timezone: string

AuthResponse
  user: UserView
  session_expires_at: ISO 8601 UTC
```

建议错误：

```text
401 AUTHENTICATION_REQUIRED：未登录、Cookie 无效或会话过期
401 INVALID_CREDENTIALS：用户名或密码错误
409 USERNAME_ALREADY_EXISTS：用户名已存在
422 INVALID_REQUEST：字段校验失败
429 TOO_MANY_ATTEMPTS：短时间登录失败过多
```

登录失败统一返回“用户名或密码错误”，不能暴露用户名是否存在。

### 3.6 现有业务 API 的调整

当前前端把固定的 `demo-user` 作为 `user_id` 发送到全部接口。账号系统上线后，后端必须从 Session 获取 `current_user_id`，不能信任客户端传入的用户 ID。

处理原则：

- `GET /api/health`、`GET /api/ready`、API 文档和注册/登录保持公开。
- `/api/chat`、反馈、提醒、记忆和指标接口全部要求登录。
- 使用 Cookie 认证的 `POST`、`PATCH` 和 `DELETE` 请求校验 `Origin`，只接受配置的前端来源；非浏览器测试客户端必须显式使用受信来源。
- Service 和 Repository 内部继续显式接收 `user_id`，便于隔离和测试。
- HTTP 请求模型和 Query 中移除 `user_id`；由路由层注入当前用户 ID。
- 如果为兼容临时保留了客户端 `user_id`，后端也必须忽略它或校验与当前用户一致，绝不能直接采用。
- 前端删除 `USER_ID = 'demo-user'`，统一从认证状态读取当前用户。

本项是安全核心，不得只保护注册和登录接口而让提醒、记忆等接口继续接受任意 `user_id`。

## 4. 三人职责与文件边界

### 4.1 队长：API 合同、认证接入和总体集成

负责事项：

1. 更新 `API_SPEC.md`，冻结认证接口、错误码、Cookie 和现有接口的用户身份规则。
2. 增加认证 Pydantic 模型。
3. 增加认证 API 路由和 `get_current_user` 依赖。
4. 将认证依赖接入聊天、反馈、提醒、记忆和指标路由。
5. 移除 HTTP 层对客户端 `user_id` 的信任。
6. 在 `main.py` 注册认证路由，并核对 CORS、Cookie、写请求来源校验和异常处理。
7. 增加 `pwdlib[argon2]` 依赖并更新锁定文件。
8. 维护 `.env.example`、README 和部署配置说明。
9. 编写跨用户隔离和完整认证 API 集成测试。
10. 负责最终前后端联调、云端 HTTPS 验证和代码冻结。

主要文件：

```text
API_SPEC.md
requirements.txt
requirements-lock.txt
.env.example
README.md
backend/app/main.py
backend/app/api/auth.py
backend/app/api/dependencies.py
backend/app/api/errors.py
backend/app/api/chat.py
backend/app/api/feedback.py
backend/app/api/reminders.py
backend/app/api/memories.py
backend/app/api/metrics.py
backend/app/schemas/auth.py
backend/app/schemas/现有请求模型
backend/tests/api/
```

队长不修改队员 A 的 Repository 和 Service 实现，也不修改队员 B 的前端页面。

### 4.2 队员 A：数据库、密码和 Session 服务

负责事项：

1. 增加数据库 V3 迁移，扩展 `users` 并创建 `auth_sessions`。
2. 保证旧数据库升级时不丢失提醒、记忆、消息和指标。
3. 扩展 `UserRepository`，支持按规范化用户名查询和创建真实账号。
4. 新增 `AuthSessionRepository`，支持创建、查询、撤销和清理过期会话。
5. 新增 `AuthService`，实现注册、密码校验、登录、当前会话解析和退出。
6. 使用 Argon2id 哈希密码，禁止记录密码和密码哈希。
7. 使用安全随机数生成 Session Token，数据库只写入哈希。
8. 实现最小登录失败限制：同一规范化用户名连续失败 5 次后暂停 15 分钟；成功登录后清零。所有失败响应保持一致，避免暴露账号是否存在。
9. 删除“任意 user_id 自动创建用户”的生产路径；用户只能通过注册创建。
10. 编写迁移、Repository 和 Service 单元测试。

必须覆盖的测试：

- 旧 V2 数据库能够升级到 V3，原数据数量和归属不变。
- 用户名大小写和首尾空格规范化后仍保持唯一。
- 数据库中没有明文密码。
- 正确密码登录成功，错误密码返回相同的通用错误。
- Session 原始 Token 不进入数据库。
- 会话在 180 天边界前有效，边界到达后无效。
- 退出后 Session 立即无效。
- 两台设备的 Session 相互独立。
- 登录失败限制和成功后的计数清零有效。
- 过期 Session 清理不会删除用户业务数据。

主要文件：

```text
backend/app/database.py
backend/app/repositories/users.py
backend/app/repositories/auth_sessions.py
backend/app/repositories/__init__.py
backend/app/services/auth_service.py
backend/app/services/__init__.py
backend/tests/repositories/
backend/tests/services/
backend/tests/test_database.py
```

队员 A 不修改 Agent Runtime、API 路由、前端或依赖文件。需要新依赖时先通知队长。

### 4.3 队员 B：注册登录页面和前端身份切换

负责事项：

1. 实现登录和注册页面，不增加营销落地页。
2. 增加认证状态管理：启动时调用 `/api/auth/me`，区分加载中、已登录和未登录。
3. 在 API Client 中增加注册、登录、当前用户和退出方法。
4. 所有请求使用 `credentials: 'include'` 携带 Cookie。
5. 删除固定 `USER_ID`，由当前用户状态驱动页面。
6. 更新对话、提醒、记忆、指标和到期轮询的 API 参数。
7. 登录成功后进入现有主界面，退出后立即停止提醒轮询并回到登录页。
8. 按真实用户 ID 隔离本地聊天历史，避免账号切换后看到上一账号的本地记录。
9. 登录过期收到 `401` 时统一清除认证状态并跳转登录页。
10. 实现密码显示切换、确认密码、加载状态和明确错误提示。
11. 使用标准 `autocomplete` 属性支持浏览器密码管理器，前端不自行保存密码。
12. 完成桌面和移动端适老化测试。

必须验证：

- 刷新页面不会退出登录。
- 关闭并重新打开浏览器后，在 Cookie 未过期时可以恢复登录。
- 注册、登录重复点击不会重复请求。
- 用户名已存在、密码错误和网络失败有清晰提示。
- 退出后无法继续看到或请求前一账号的数据。
- 切换账号后本地聊天历史不串号。
- Session 过期时不会陷入重复请求或跳转循环。
- 手机软键盘不会遮挡注册和登录按钮。

主要文件：

```text
frontend/src/api/client.js
frontend/src/api/constants.js
frontend/src/App.jsx
frontend/src/App.css
frontend/src/pages/AuthPage.jsx
frontend/src/认证状态相关文件
frontend/src/pages/ChatPage.jsx
frontend/src/pages/RemindersPage.jsx
frontend/src/pages/MemoriesPage.jsx
frontend/src/pages/MetricsPage.jsx
frontend/src/ReminderAlarm.jsx
```

队员 B 不修改后端 Schema、Service、数据库和 API 合同。发现接口问题先通知队长。

## 5. 总开发顺序

### 阶段 0：共同确认需求

参与：全员，不并行。

1. 确认账号字段只包含用户名、密码、显示名和时区。
2. 确认六个月定义为固定 180 天，不滑动续期。
3. 确认前后端生产环境使用同一站点和 HTTPS。
4. 确认旧 `demo-user` 数据保留但不能用默认密码登录。
5. 确认所有业务 API 改为从 Session 取得用户 ID。
6. 确认本轮不做找回密码；测试账号遗忘密码时直接重建测试数据库或由管理员离线处理。

输出：冻结的需求边界。

### 阶段 1：队长冻结合同和骨架

参与：队长，其他人暂不改代码。

1. 更新 `API_SPEC.md`。
2. 新增认证 Schema 和 API 路由骨架。
3. 冻结 `AuthService` 和 Repository 方法签名。
4. 增加 `pwdlib[argon2]` 依赖。
5. 在 `.env.example` 增加 Cookie 和会话配置。
6. 添加最小 Mock 或空实现，使后端仍能启动。
7. 运行现有测试并推送第一个基准提交。

队长通知两位队员拉取该提交后，才能进入并行开发。

### 阶段 2：第一轮并行开发

以下三组同时进行，严格遵守文件边界。

#### 队员 A：认证数据与服务

1. 实现 V3 数据库迁移。
2. 实现用户和 Session Repository。
3. 实现密码哈希、注册、登录、会话验证和退出 Service。
4. 实现失败登录限制和过期会话清理。
5. 完成 Repository 和 Service 测试。

#### 队员 B：认证前端

1. 根据已冻结 API 使用 Mock 实现登录和注册页面。
2. 实现认证状态和页面门禁。
3. 修改 API Client，统一携带 Cookie。
4. 删除固定 `demo-user`，让所有页面使用当前用户。
5. 实现退出、401 处理和本地聊天历史隔离。
6. 完成前端交互测试、lint 和构建。

#### 队长：认证 API 与业务保护

1. 完成认证路由、Cookie 设置和删除。
2. 实现 `get_current_user` 依赖。
3. 给全部业务路由接入认证依赖。
4. 从 HTTP 请求模型中移除 `user_id`，在路由层注入用户 ID。
5. 添加 401、409 和 429 统一错误处理。
6. 编写 API 测试骨架和跨用户隔离用例。

### 阶段 3：第一轮顺序合入

三人都直接推送 `main` 时，严格按以下顺序：

#### 第一个：队员 A

1. 通知群内暂停推送。
2. 拉取最新 `main` 并处理自己文件范围内的变化。
3. 运行数据库、Repository 和 Service 测试。
4. 只提交队员 A 负责的文件并推送。
5. 通知队长提交号、迁移版本和 `AuthService` 实际签名。

#### 第二个：队长

1. 拉取队员 A 的提交。
2. 将认证 API 接入真实 `AuthService`。
3. 完成所有业务路由的用户身份注入。
4. 更新 API 测试和文档。
5. 运行完整后端测试并推送。
6. 通知队员 B 最终接口状态。

#### 第三个：队员 B

1. 拉取队长的提交。
2. 将 Mock 替换为真实认证 API。
3. 修正最终字段和错误状态。
4. 运行 lint、生产构建和基础浏览器测试。
5. 只提交前端文件并推送。

### 阶段 4：端到端联调

参与：全员，可并行验证，问题按文件负责人修复。

#### 队员 A

- 验证旧数据库迁移和密码、Session 数据安全。
- 检查会话过期、退出和并发登录。
- 检查生产路径不会自动创建任意用户。

#### 队员 B

- 验证注册、登录、自动恢复、退出和 401 跳转。
- 验证手机与桌面布局。
- 验证账号切换后历史和提醒不串号。

#### 队长

- 验证所有受保护 API 未登录返回 401。
- 使用两个测试账号验证数据完全隔离。
- 验证 Cookie 属性、CORS、写请求来源校验和 HTTPS 部署行为。
- 运行 Agent、反馈、提醒和记忆的完整回归。

建议端到端顺序：

1. 注册账号 A，确认自动登录。
2. 账号 A 创建提醒和偏好记忆。
3. 退出账号 A，确认业务请求返回 401。
4. 注册账号 B，确认看不到账号 A 的数据。
5. 账号 B 创建自己的提醒。
6. 重新登录账号 A，确认只能看到账号 A 的数据。
7. 刷新页面和重启浏览器，确认登录状态恢复。
8. 模拟 Session 过期，确认自动回到登录页。
9. 确认数据库没有明文密码和原始 Session Token。
10. 继续跑原有反馈记忆与提醒演示，确认账号改造没有破坏主功能。

### 阶段 5：第二轮并行修复

参与：全员，并行开发、顺序推送。

- 队员 A 只修复数据库、Repository、AuthService 和迁移问题。
- 队员 B 只修复认证页面、状态管理和前端串号问题。
- 队长只修复 API、权限隔离、Cookie、部署和跨模块集成问题。

推送顺序仍为：队员 A、队长、队员 B。

### 阶段 6：云端部署和最终验证

#### 队长先执行

1. 配置持久化 SQLite 磁盘。
2. 配置 HTTPS 和同站点反向代理。
3. 设置生产环境变量。
4. 执行数据库迁移并检查 `/api/ready`。

#### 全员并行验证

- 队员 A：检查生产数据库迁移、Session 过期字段和敏感数据。
- 队员 B：在真实手机和桌面浏览器检查持久登录、退出和适老界面。
- 队长：检查安全 Cookie、跨用户隔离、日志脱敏和完整业务回归。

全部通过后冻结代码，禁止继续增加账号功能。

## 6. 环境变量

建议新增：

```text
SESSION_TTL_DAYS=180
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_NAME=yoko_session
FRONTEND_ORIGIN=http://127.0.0.1:5173
```

生产环境：

```text
SESSION_TTL_DAYS=180
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_NAME=__Host-yoko_session
FRONTEND_ORIGIN=https://实际域名
```

环境变量中不需要保存用户密码或 Session Token。`.env` 不得提交。

## 7. 完成标准

只有同时满足以下条件，账号系统才算完成：

- 注册、登录、当前用户和退出接口均有明确输入输出文档。
- 密码只保存 Argon2id 哈希。
- Session Token 只在浏览器 Cookie 中保存原值，数据库只保存哈希。
- Cookie 在生产环境启用 `HttpOnly`、`Secure` 和 `SameSite=Lax`。
- Session 固定 180 天后过期，过期后必须重新登录。
- 所有业务 API 从服务端 Session 获取用户 ID。
- 两个账号无法互相读取或修改对方数据。
- 前端不再包含固定 `demo-user`。
- 浏览器刷新和重启后能够自动恢复未过期登录。
- 退出或过期后提醒轮询停止，页面回到登录状态。
- 旧数据库升级后原有业务数据不丢失。
- 后端完整测试、前端 lint 和生产构建通过。
- 云端 HTTPS 环境完成一次真实端到端验证。

## 8. 协作规则

- 每人开始前执行 `git pull --ff-only`。
- 三人并行时禁止修改他人的文件范围。
- 准备推送前在群内声明，同一时间只允许一人推送。
- 只添加本人文件，不使用 `git add .`。
- 禁止强制推送，禁止用旧版本整体覆盖文件。
- 数据库迁移一旦推送，不得修改原迁移内容，只能增加新迁移。
- 接口变更必须先更新 `API_SPEC.md` 并通知全员。
- 不提交 `.env`、数据库、Cookie、Token、密码、日志或测试账号真实密码。

## 9. 参考依据

- [FastAPI：密码哈希与认证](https://fastapi.tiangolo.com/tutorial/security/oauth2-jwt/)
- [Starlette：Cookie 设置接口](https://www.starlette.io/responses/)
- [OWASP：Session 管理](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)
- [OWASP：密码存储](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)

本方案使用 FastAPI 官方推荐的 `pwdlib` 和 Argon2，并遵循 OWASP 对密码哈希、随机 Session、`HttpOnly`、`Secure`、`SameSite` 和服务端访问控制的基本要求。
