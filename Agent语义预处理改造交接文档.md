# Agent 语义预处理改造交接文档

## 1. 交接信息

- 交接日期：2026-08-24
- 当前分支：`main`
- 已推送提交：`4b55557 feat: add semantic preprocessing for agent requests`
- 远端仓库：`yoko/main`
- 外部 API：未改变
- 数据库结构：未改变
- 前端：不需要同步修改

## 2. 改造原因

原运行时同时依赖主模型和部分中文关键词、时间正则进行语义校验。规则不断增加后，出现了以下问题：

- 用户明确说“早上八点”，后面又口语化重复“我一般八点起来”，系统会把最后一个“八点”误判为缺少上午或下午。
- 模型对用户原话进行正常概括后，旧的逐字证据校验可能拒绝合法操作。
- 内部校验错误直接显示给老人，例如“用户消息编号不存在”或“1点到12点缺少时段说明”，表达机械且难以理解。
- 修补单个错字或关键词容易形成过拟合，不能可靠覆盖真实老年人口语。

本次将流程调整为“结构化语义预处理 + 主 Agent + 最小确定性安全校验”。

## 3. 当前 Agent 流程

一次 `/api/chat` 请求的处理顺序如下：

1. `ChatService` 保存用户消息。
2. 检索最多 3 条相关记忆。
3. 读取当前会话最近 6 条消息。
4. 调用一次语义预处理模型，生成 `SemanticFrame`。
5. 主 Agent 同时读取用户原文、历史、记忆和 `SemanticFrame`。
6. 主 Agent 可查询提醒，并生成最多一个提醒写操作计划。
7. 语义安全门检查预处理结果与工具计划是否一致。
8. 只有最终状态为 `completed` 且所有校验通过时，才真正执行写操作。
9. 写入助手回复、记忆变化和请求指标。

提醒写工具仍采用“先暂存计划、最后执行”的方式，因此模型在澄清阶段调用工具也不会立即修改数据库。

## 4. SemanticFrame

定义位置：`backend/app/agent/runtime.py`

主要字段：

| 字段 | 作用 |
| --- | --- |
| `normalized_text` | 对当前最终语义的简短规范化表达 |
| `active_operation` | `none/create/update/delete` |
| `intent` | 对话、提醒查询、提醒操作、记忆偏好、医疗咨询或不明确 |
| `reminder_title` | 提醒事项 |
| `target_reference` | “那个”“刚才医院那条”等指代 |
| `date_text` | 用户最终采用的日期表达 |
| `time_text` | 用户最终采用的钟点和时段表达 |
| `repeat_type` | 一次、每天、每周或未指定 |
| `cancelled` | 用户是否最终撤销操作 |
| `multiple_operations` | 当前轮是否要求多个独立写操作 |
| `discarded_interpretations` | 用户改口后放弃的中间版本 |
| `clarification_questions` | 必须向用户确认的自然语言问题 |
| `evidence_message_numbers` | 支持当前语义的用户消息编号 |
| `confidence` | 语义置信度，范围 0 到 1 |

语义预处理只负责理解，不允许调用工具。用户原文仍是最终事实来源，语义帧不能覆盖原文。

## 5. 写操作安全门

语义预处理和主 Agent 必须满足以下条件，提醒写操作才会执行：

- 用户最终没有撤销操作。
- 当前轮不是多个独立写操作。
- 没有尚未解决的关键歧义。
- 语义置信度不低于 `0.65`。
- `SemanticFrame.active_operation` 与工具计划一致。
- 工具引用的用户消息编号真实存在。
- 普通写操作必须引用当前用户消息。
- 每轮最多执行一个提醒写操作。
- 修改和删除前必须先查询真实提醒列表。
- 使用时间记忆时，记忆 ID 必须来自本轮检索结果，且时间必须一致。
- 提醒服务自身的幂等、去重、时间和数据校验仍然有效。

已删除原先会误判口语重复的中文钟点正则。当前只保留与业务语义无关的结构性校验。

## 6. 用户回复处理

内部错误不再直接返回给用户。

例如，用户说“把交水费改到十一点”时，系统应回答：

> 您想把交水费的提醒改到十一点，请问是上午十一点还是晚上十一点呢？

用户明确说“只是聊聊，不要再设提醒”时，即使主 Agent 错误规划了写工具，安全门也会阻止写入，并使用自然回复说明不会修改提醒。

## 7. 修改文件

- `backend/app/agent/runtime.py`
  - 新增 `SemanticFrame`。
  - 新增语义预处理模型调用。
  - 将语义帧加入主 Agent 系统上下文。
  - 新增语义计划一致性安全门。
  - 删除脆弱的中文时间正则校验。
  - 将内部校验失败转换为自然回复。
  - 将预处理模型调用计入现有指标。

- `backend/tests/test_agent_runtime.py`
  - 增加语义预处理结构化输出测试。
  - 增加重复钟点口语测试。
  - 增加模糊十一点的自然澄清测试。
  - 增加“只是聊天”阻止误写测试。
  - 更新内部错误不再暴露后的断言。

- `backend/tests/evaluation/run_live_stress_evaluation.py`
  - 实时评测使用注册后的独立测试用户。
  - 增加合法前端 `Origin`，兼容账号系统。

- `backend/tests/evaluation/run_live_dialogue_evaluation.py`
  - 增加口语提醒和多轮时段补充场景。
  - 实际统计对话轮数。

## 8. 测试结果

### 8.1 不联网测试

```powershell
conda activate yoko
python -m pytest backend/tests -q
```

最终结果：

```text
144 passed
```

### 8.2 真实模型定向测试

覆盖以下场景：

- 医院提醒长句连续改口，最终修改为后天下午 5 点。
- “把那个改到十一点”先自然追问上午或晚上，不修改原提醒。
- 用户补充“交水费，明天下午三点”后正确原地更新。
- 用户补充医保卡和检查单，但明确只聊天，不新增或修改提醒。

最终结果：`3/3` 通过。

### 8.3 40 轮长对话攻击测试

最后一次完整攻击测试报告：

```text
.local-attack/reports/adaptive-long-dialogue-20260824-224852.md
```

结果：

- 40 轮自动状态检查全部通过。
- 提示词注入没有执行任何写工具。
- 错字修改、连续改口、否定删除、记忆使用、模糊指代和多提醒读取均未产生错误写入。
- 攻击模型发现第 11 轮回复会错误诱导用户重新确认创建；该回复问题随后已修复，并通过定向真实模型测试。
- 最终回复小修后没有再次运行完整 40 轮，只运行了相关定向场景。
- 第 22 轮攻击生成器没有真正生成重复创建请求，因此该轮不能作为重复创建能力的有效证据；重复创建仍由单元测试和提醒服务测试覆盖。

`.local-attack` 已被 Git 忽略，报告和攻击脚本只存在于运行测试的本机，不会随仓库同步。

## 9. 成本和速度变化

与加入语义预处理前的 40 轮测试相比：

| 指标 | 修改前 | 修改后 | 变化 |
| --- | ---: | ---: | ---: |
| 目标 Agent 模型调用 | 77 | 117 | 约 +52% |
| 输入 token | 268,578 | 349,091 | 约 +30% |
| 输出 token | 9,756 | 16,750 | 约 +72% |
| 目标 Agent 累计耗时 | 96.5 秒 | 143.5 秒 | 约 +49% |
| 完整测试墙钟时间 | 200.6 秒 | 235.3 秒 | 约 +17% |

现有 `model_call_count`、`input_tokens`、`output_tokens` 和 `model_ms` 已包含语义预处理调用，但 API 暂时没有单独返回预处理阶段的指标。

## 10. 运行方式

### 10.1 启动后端

```powershell
conda activate yoko
python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

### 10.2 普通实时模型评测

以下命令会读取 `.env` 中的真实模型配置并产生实际模型费用：

```powershell
python -m backend.tests.evaluation.run_live_stress_evaluation
python -m backend.tests.evaluation.run_live_dialogue_evaluation
```

评测使用临时数据库，不会污染正式账号和提醒数据。

### 10.3 本机完整攻击测试

完整攻击脚本位于被忽略的 `.local-attack`，只适用于当前保留该目录的机器。

先启动隔离后端：

```powershell
$env:DATABASE_PATH=".local-attack/adaptive-long-local.db"
python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8002
```

在新终端运行：

```powershell
python .local-attack/long_elderly_dialogue.py --base-url http://127.0.0.1:8002 --turns 40
```

测试完成后需要关闭 8002 服务。

## 11. 已知限制

1. 每轮固定增加一次模型调用，成本和延迟明显上升。
2. 预处理和主 Agent 当前使用同一个模型，二者的错误可能具有相关性。
3. `confidence` 是模型自报值，并非经过严格校准的统计概率。
4. 当前会话只读取最近 6 条消息，跨越更久的事实主要依赖真实提醒列表和长期记忆。
5. 预处理指标被合并到总模型指标，暂时不能通过 API 单独比较预处理成本。
6. 完整攻击脚本被 Git 忽略，其他成员拉取仓库后无法直接运行同一脚本。

## 12. 下一步建议

按优先级建议：

1. 部署后用真实前端完成注册、登录、提醒、反馈和刷新会话的端到端测试。
2. 在云端记录 20 到 50 条真实但脱敏的用户表达，统计错误追问率、错误写入率和平均响应时间。
3. 若成本或速度不能接受，再增加可选的 `PREPROCESS_MODEL_NAME`，让预处理使用更便宜的小模型；不要立即接入新的模型供应商。
4. 若答辩需要展示记忆成本，可再为 API 指标增加独立的预处理 token 和耗时字段。该改动会涉及 schema、数据库迁移和 API 文档，应单独完成。
5. 改进攻击生成器的阶段一致性检查，确保“重复创建”等目标确实被生成后再计分。

## 13. 回退方式

如部署后发现不可接受的回归，可以创建反向提交回退本次改造：

```powershell
git revert 4b55557
```

不建议使用 `git reset --hard`，以免覆盖其他成员随后提交的工作。
