# Yoko 图片模态三人开发分工

当前进度：阶段 1 接口契约已完成；带图请求在视觉服务接通前明确返回 `503 MODEL_UNAVAILABLE`，不会静默忽略图片。

## 一、目标与范围

本阶段只实现轻量版图片理解：

- 每轮最多上传 1 张图片。
- 支持 JPEG、PNG、WebP，原图最大 5 MiB。
- 使用 `deepseek-v4-flash-vision-exp` 完成图片理解。
- 视觉模型只负责提取图片内容，现有 Agent 继续负责对话、记忆、联网和提醒工具调用。
- 第一版不长期保存原始图片，只保存必要的结构化摘要、置信度和图片哈希。
- 图片识别出的提醒、药品用法和日期不能直接执行，必须经过现有 Agent 判断；信息不确定或涉及用药时必须向用户确认。

## 二、职责划分

### 队长：接口、安全规则和最终集成

负责整体设计、公共接口和最终验收，避免前后端根据不同假设开发。

需要完成：

1. 在 `API_SPEC.md` 中确定图片请求和错误响应格式。
2. 修改 `backend/app/schemas/chat.py`，增加可选图片请求模型。
3. 确定视觉服务的内部调用接口，例如 `analyze(image, message) -> VisionObservation`。
4. 修改 `backend/app/services/chat_service.py`，把视觉分析结果传给 Agent。
5. 必要时修改 `backend/app/agent/runtime.py`，将图片分析结果作为不可信参考信息加入当前轮上下文。
6. 增加图片大小、格式、Base64、像素尺寸和提示词注入防护。
7. 更新 `.env.example`、`requirements.txt` 和 `requirements-lock.txt`。
8. 编写 API、Agent 集成、幂等和安全测试。
9. 拉取队员 A、B 的提交，执行最终联调和全量回归测试。

主要负责文件：

- `API_SPEC.md`
- `backend/app/schemas/chat.py`
- `backend/app/api/chat.py`
- `backend/app/services/chat_service.py`
- `backend/app/agent/runtime.py`
- `backend/app/config.py`
- `.env.example`
- `requirements.txt`
- `requirements-lock.txt`
- 对应集成测试文件

### 队员 A：视觉模型适配与后端单元测试

负责把图片安全地转换为结构化视觉信息，不负责提醒创建等业务决策。

需要完成：

1. 新建独立的视觉分析模块，例如 `backend/app/services/vision_service.py`。
2. 使用现有 `ChatOpenAI` 调用 `deepseek-v4-flash-vision-exp`。
3. 传入用户文字和图片 Data URL，并使用结构化输出。
4. 输出图片摘要、可见文字、可能的日期、置信度和风险提示。
5. 对模型超时、限流、无效响应和结构化解析失败做统一处理。
6. 确保日志中不记录 API Key、Base64 图片或完整敏感识别结果。
7. 使用模拟模型编写单元测试，不让普通 CI 依赖真实 API。
8. 提供一个可手动运行的真实模型冒烟测试，但默认不在 CI 中执行。

主要负责文件：

- `backend/app/services/vision_service.py`
- 可选的内部视觉数据模型文件
- `backend/tests/services/test_vision_service.py`
- 视觉服务相关测试工具

队员 A 不修改公共聊天请求模型、前端文件和现有提醒逻辑。

### 队员 B：图片上传与前端交互

负责用户选择图片、预览、发送以及错误提示。

需要完成：

1. 在聊天输入区增加图片选择按钮，移动端允许调用后置摄像头。
2. 只允许选择 JPEG、PNG、WebP。
3. 在发送前检查文件大小，超过 5 MiB 时给出清晰提示。
4. 展示稳定尺寸的图片预览、文件名和移除按钮。
5. 将图片转换为 Base64，并按 API 文档加入聊天请求。
6. 请求发送期间禁用重复选择和重复提交。
7. 发送失败后保留图片，允许用户重试；发送成功后清空图片。
8. 聊天气泡只显示本轮本地预览，第一版刷新页面后不恢复原图。
9. 为编码、大小检查、发送和重试逻辑补充前端测试。

主要负责文件：

- `frontend/src/pages/ChatPage.jsx`
- `frontend/src/api/client.js`
- 图片处理工具及其测试文件
- 聊天页相关样式文件

队员 B 不修改后端模型和 Agent 逻辑。

## 三、开发总流程

### 阶段 0：同步基线（全员，串行开始）

1. 队长确认远端 `main` 测试通过。
2. 全员执行 `git pull --ff-only yoko main`。
3. 全员确认 `.env`、数据库文件、图片样本和真实 API Key 均被 Git 忽略。

### 阶段 1：冻结接口（队长，必须先完成）

1. 确定 `ChatRequestBody.image` 的字段、限制和错误码。
2. 确定 `VisionObservation` 的内部字段和视觉服务方法签名。
3. 更新 API 文档、请求模型和基础契约测试。
4. 队长提交并推送接口基线。
5. 队员 A、B 拉取该提交后开始实现。

接口基线完成前，队员 A、B只能准备测试图片和阅读代码，不能自行定义另一套字段。

### 阶段 2：并行开发

以下工作同时进行：

- 队员 A：视觉模型适配、结构化输出、异常处理、后端单元测试。
- 队员 B：上传按钮、预览、Base64 转换、请求发送、前端测试。
- 队长：图片安全校验、ChatService 接入、Agent 上下文注入、集成测试。

三人必须遵守上面的文件归属。确需修改他人负责文件时，先在群内说明修改位置和原因。

### 阶段 3：依次推送

由于直接向 `main` 推送，任何时刻只能有一人执行推送，禁止使用强制推送。

1. 队员 A 先提交视觉服务和单元测试，执行后端相关测试后推送。
2. 队员 B 执行 `git pull --rebase yoko main`，解决本地基线落后问题，运行前端测试和构建后推送。
3. 队长执行 `git pull --rebase yoko main`，合入两人的成果，完成接口连接和必要修正。
4. 若 `rebase` 出现同一业务逻辑冲突，停止推送并由对应文件负责人确认，禁止直接选择“全部接受”。

建议每个提交只包含本人负责的图片模态文件，不混入 `.env`、数据库、测试图片或无关格式化修改。

### 阶段 4：队长集成测试

按以下顺序验证：

1. 无图片的原有聊天、提醒、记忆和联网功能没有回归。
2. 正常 JPEG、PNG、WebP 可以识别并回答。
3. 超大图片、伪造扩展名、损坏 Base64 和不支持格式会被拒绝。
4. 模糊药盒、手写内容和低置信度日期会触发自然确认，不直接创建提醒。
5. 图片中的提示词注入不会覆盖系统规则或调用危险工具。
6. 相同幂等键重试不会重复创建消息或提醒。
7. 模型超时、限流和返回格式错误时，用户能收到简短可理解的提示。
8. 后端全量测试、前端测试、静态检查和生产构建全部通过。
9. 使用真实模型完成一轮受控冒烟测试，确认 Token、耗时和日志均正常。

### 阶段 5：最终提交与交接（队长）

1. 更新 README、API 文档、环境变量说明和测试方法。
2. 检查 `git diff --check` 和 `git status`。
3. 提交集成修改并推送 `main`。
4. 记录当前限制：单张图片、5 MiB、不长期保存原图、实验模型可能变化。

## 四、建议请求格式

```json
{
  "conversation_id": null,
  "message": "帮我看看这个药盒上写的用法",
  "timezone": "Asia/Shanghai",
  "image": {
    "media_type": "image/jpeg",
    "data": "<纯 Base64 数据，不包含 data URL 前缀>",
    "detail": "original"
  }
}
```

原有纯文本请求不携带 `image`，行为保持不变。

## 五、完成标准

- 原有 API 保持向后兼容。
- 图片只被视觉模型分析一次，不在多个 Agent 步骤中重复发送。
- 图片内容不能直接绕过提醒确认和工具安全规则。
- Base64、原图和密钥不进入日志及 Git 仓库。
- 模拟测试可以在无 DeepSeek Key 的 CI 环境运行。
- 真实模型测试有明确开关，不会在日常测试中产生意外费用。
