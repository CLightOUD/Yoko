# Yoko 后端阶段交接文档

更新时间：2026-08-23
交接范围：后端 Agent、反馈记忆、提醒服务、API、测试与接口文档
当前分支：`main`

## 0. 任务概览

P0 已完成，并与现有 FastAPI、SQLite、Service、Schema 及前端调用方式保持兼容。当前改动保留在工作区，尚未提交或推送。

主要完成：

- [数据库迁移]：加入迁移版本表、V1/V2 顺序迁移、事务回滚、旧库自动备份、新版本数据库拒绝降级启动。
- [Chat幂等与一致性]：支持 Idempotency-Key，防止重复消息、Agent 调用和指标写入；失败重试复用原请求和用户消息；收尾写入统一事务提交。
- [Readiness]：新增 /api/ready，检查数据库连接、Schema 版本及全部核心表；原 /api/health 保持不变。
- [安全日志]：加入 JSON 结构化日志，仅输出白名单上下文，不记录请求体、密钥或任意敏感字段。
- 安全错误响应：模型、工具、数据库和未知异常不再向客户端泄露底层错误；422 响应不回显原始输入。
- [前端调用]：聊天自动生成并发送幂等键，应用状态改用 readiness。
- [API文档]及[交接文档]已同步。

验证结果：

- 后端：105 passed in 5.10s
- Python 编译检查：通过
- 依赖检查：No broken requirements found
- 前端 lint：通过；保留 3 条已有的 effect 警告
- 前端生产构建：通过，1811 个模块完成构建
- Git 差异检查：无空白错误
- 未调用真实模型服务，未改动真实业务数据库，未提交或推送代码

## 1. 当前状态

当前后端功能已经完成 P0 可靠性加固，并通过离线自动化测试。前端已接入聊天、提醒、记忆和指标基础页面；本次未调用真实模型。

已完成内容：

- 基于 FastAPI、LangChain、Pydantic 和 SQLite 的基础 Agent 流程。
- 用户消息、Agent 回复和会话历史持久化。
- 反馈偏好提取、记忆写入、覆盖、停用、检索和使用记录。
- 一次性、每日和每周提醒。
- 提醒创建、查询、修改、删除、到期查询和幂等确认。
- 重复提醒合并、周期覆盖和历史重复数据清理。
- 请求耗时、Token、记忆检索量和记忆使用量统计。
- 真实模型冒烟测试、严苛单轮测试和高难度多轮对话测试。
- 版本化 SQLite 迁移、旧库升级前备份与迁移失败回滚。
- Chat 请求幂等键、执行租约、失败重试与收尾事务一致性。
- 进程存活检查、数据库就绪检查、结构化请求日志和安全错误消息。

## 2. 主要目录

```text
backend/app/agent/          Agent 运行时、提示词、偏好提取
backend/app/api/            FastAPI 路由与错误处理
backend/app/repositories/   SQLite 数据访问
backend/app/schemas/        Pydantic 请求和响应模型
backend/app/services/       Chat、反馈、记忆、提醒和指标业务逻辑
backend/tests/api/          API 集成测试
backend/tests/evaluation/   规则评测与真实模型评测脚本
backend/tests/services/     Service 测试
API_SPEC.md                 接口契约和行为说明
README.md                   环境、启动和测试命令
```

## 3. API 概览

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/health` | 进程存活检查 |
| `GET` | `/api/ready` | 数据库连接与迁移版本就绪检查 |
| `POST` | `/api/chat` | Agent 对话、记忆检索和工具调用 |
| `POST` | `/api/feedback` | 记录反馈并写入或更新记忆 |
| `GET` | `/api/memories` | 查询记忆 |
| `PATCH` | `/api/memories/{id}` | 修改或停用记忆 |
| `DELETE` | `/api/memories/{id}` | 停用记忆 |
| `POST` | `/api/reminders` | 创建提醒 |
| `GET` | `/api/reminders` | 查询提醒 |
| `GET` | `/api/reminders/due` | 查询到期提醒 |
| `PATCH` | `/api/reminders/{id}` | 修改提醒 |
| `DELETE` | `/api/reminders/{id}` | 删除提醒 |
| `POST` | `/api/reminders/{id}/ack` | 确认提醒并推进周期 |
| `GET` | `/api/metrics/summary` | 查询 Agent 和记忆指标 |

所有字段、输入输出示例、状态码和错误格式以 `API_SPEC.md` 为准。

## 4. Agent 与记忆规则

### 4.1 记忆流程

1. `/api/chat` 根据任务类型检索最多 3 条相关记忆。
2. Agent 只在记忆实际影响回复或工具参数时标记 `used=true`。
3. 明确的长期表达可以直接通过聊天写入记忆。
4. `/api/feedback` 可以在一次反馈中写入多项不同偏好。
5. 同一 `task_type + memory_key` 只保留一个有效值，新反馈更新原记录。
6. 否定、临时状态和不明确表达不会被错误写为长期偏好。

当前支持的主要偏好包括：

- 全局回复风格：简短或详细。
- 全局语言：中文。
- 服药、散步和预约的默认提醒时间。
- 预约提前量。

### 4.2 Chat 幂等与一致性

- 客户端可通过 `Idempotency-Key` 标识同一次聊天请求；同一成功请求重试返回原响应。
- 同一键不能配合不同请求体；仍在租约期内的请求拒绝并发重复执行。
- 失败请求复用原 request ID、conversation ID 和用户消息重新执行。
- 记忆使用、偏好更新、助手消息、指标和最终响应在同一事务中提交。
- 模型调用期间不持有 SQLite 长事务；当前提醒创建工具继续由 `ReminderService` 去重。

### 4.3 确定性快速路径

- 日期明确但缺少钟点，且存在相关 `preferred_time` 时，直接创建提醒，不调用模型。
- 只有“上午”“下午”“过会儿”等范围且没有时间记忆时，直接追问具体钟点。
- 多轮补充会继承上一轮正在询问提醒时间的语境。
- 查询、确认或复述刚创建的提醒时，不重复调用创建工具。
- 本轮明确给出钟点时，旧 `preferred_time` 不算实际使用，也不会覆盖长期记忆。

### 4.4 输入错误处理

规则层只修正常见且无歧义的输入错误，例如：

- `晚丄` 转为 `晚上`。
- `早丄` 转为 `早上`。
- `7典` 转为 `7点`。
- `提酲` 转为 `提醒`。

不得通过规则猜测药名、药量或医疗事项。

## 5. 提醒规则

- `repeat_type` 支持 `none`、`daily` 和 `weekly`。
- 同一用户、触发时间、时区和周期只允许一条活动提醒。
- 相同时间和周期的不同内容使用中文分号合并。
- 高置信度同义内容会去重，例如“吃降压药”和“服用降压药”。
- 不同药品不会因为动词相同而合并。
- 跨周期的相同内容按 `daily > weekly > none` 保留覆盖更强的提醒。
- 周期提醒延迟确认时会推进到当前时间之后的下一次有效触发时间。
- 每日和每周推进按提醒时区的本地自然日期计算，兼容夏令时。
- 确认、删除和重复反馈均保持幂等。

## 6. 测试结果

最后一次离线全量测试：

```text
105 passed in 5.10s
compileall passed
git diff --check passed
frontend lint passed（保留 3 条既有 React effect 警告）
frontend production build passed
```

此前最后一次真实模型高难度多轮测试（P0 改动后尚未重跑）：

```text
对话组：6/6 通过
对话轮数：15/15 通过
模型调用：15
输入 Token：14438
输出 Token：1559
累计请求耗时：19282 ms
```

高难度测试覆盖：

- 日期、钟点和周期字段中的输入错误。
- 多轮逐步补充日期与时间。
- 同一句中多次否定和修正。
- 只确认或复述已有提醒，不重复创建。
- 带输入错误的长期偏好提取与后续使用。
- 本次明确要求覆盖旧偏好但不修改长期记忆。
- 冲突药量要求的拒绝与恢复医生方案后的正常提醒。

## 7. 运行与验证

在仓库根目录激活 Python 3.11 环境，然后运行：

```powershell
python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000
```

API 文档：<http://127.0.0.1:8000/docs>

离线测试：

```powershell
python -m pytest backend\tests -q
```

真实模型测试使用临时数据库，不会修改 `backend/data/app.db`：

```powershell
python -m backend.tests.evaluation.run_live_model_smoke
python -m backend.tests.evaluation.run_live_stress_evaluation
python -m backend.tests.evaluation.run_live_dialogue_evaluation
```

运行真实模型测试前，需要在本地 `.env` 配置：

```text
MODEL_PROVIDER
MODEL_NAME
OPENAI_API_KEY
OPENAI_BASE_URL
```

不得提交 `.env` 或在日志、文档中记录真实密钥。

## 8. 已知边界

- 当前提醒周期仅支持一次性、每日和每周，不支持任意 Cron 表达式。
- 同义提醒只采用保守规则，不使用模型自动判断医疗事项等价性。
- 反馈偏好提取以规则为主，未识别的新表达会跳过，不强行生成记忆。
- 真实模型行为仍可能受模型版本影响，修改 Prompt 或更换模型后必须重新运行真实评测。
- 当前没有后台定时推送进程；前端或调度程序需要轮询 `/api/reminders/due`。
- 前端尚未接入反馈提交、到期轮询、提醒确认和提醒修改页面。
- 未携带 `Idempotency-Key` 的聊天请求仍按独立请求处理。

## 9. 建议的下一步

1. 实现到期提醒的原子领取、投递记录、失败重试和通知 Worker。
2. 前端接入反馈、到期轮询、提醒确认和修改，并按 `id + next_trigger_at` 去重。
3. 在演示环境运行一次完整的前后端联调。
4. 演示前重新运行真实模型 `run_live_dialogue_evaluation`。

## 10. Git 协作说明

- 三名成员依次在 `main` 上提交和推送时，操作前先运行 `git pull --ff-only`。
- 推送前执行 `git status` 和完整测试，确认没有他人的未提交改动。
- 不提交 `.env`、`.vscode`、数据库文件、缓存或本地日志。
- 本阶段提交包含 `交接文档.md`，不包含此前明确排除的三个旧方案文档。
